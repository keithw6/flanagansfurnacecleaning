Montserrat and Source Sans 3, latin subset, from Google Fonts.
Both are licensed under the SIL Open Font License 1.1, which permits
redistribution: https://openfontlicense.org

They are bundled rather than fetched at render time so scenes render the same
offline, and so a network hiccup cannot silently swap the brand face for a
system serif.
