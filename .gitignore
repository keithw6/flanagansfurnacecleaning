_preview/
Thumbs.db
.DS_Store

# Client and operator documents - deliverables, not website files.
# Excluded because the repo must be public for free GitHub Pages.
# Delete these two lines if you'd rather keep them in the repo.
GBP-SETUP.md
Flanagans-GBP-Setup-Guide.pdf
# NOTE: the dist/ folders under tools/ are deliberately NOT ignored. They are
# generated, but they are also the files a non-developer is meant to download
# and open, so they have to actually be in the repository. Rebuild them with:
#   node tools/20-year-test/build-single.mjs
#   node tools/edc-builder/build-single.mjs
